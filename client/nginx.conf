server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html;
    # Fall back to index.html so client-side routes don't 404 on refresh
    try_files $uri $uri/ /index.html;
  }
}
